<h1>My Tasks</h1>
<ul>
<% @tasks.each do |task| %>
  <li>
    <% if task.completed? %>
      <input type="checkbox" checked>
    <% else %>
      <input type="checkbox">
    <% end %>
    <%= link_to task.title, task_path(task) %>
  </li>
<% end %>
</ul>
